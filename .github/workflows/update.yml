name: Atualizar Cupons

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependências
        run: |
          pip install --upgrade pip
          pip install google-auth google-auth-httplib2 google-api-python-client

      - name: Gerar index.html (Cupons)
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: python generate.py

      - name: Gerar ganhos-extras.html
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: python generate_ganhos.py

      - name: Commit e push (se houve mudança)
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html ganhos-extras.html
          git diff --cached --quiet && echo "Sem mudanças" && exit 0
          git commit -m "chore: atualiza páginas $(date +'%d/%m/%Y %H:%M') BRT"
          git pull --rebase origin main
          git push
